# 本地开发环境变量（复制为 .dev.vars 后填写，.dev.vars 已被 gitignore）
# 仅用于本地 wrangler dev；CI/线上部署在 GitHub Secrets 中配置
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=change-me
# JWT 签名密钥（必填，缺失时认证接口返回 503；线上用 GitHub Secret JWT_SECRET 注入）
JWT_SECRET=dev-only-secret-please-change
